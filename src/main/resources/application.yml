spring:
    application:
        name: youth-spring


    datasource:
        url: jdbc:mysql://43.201.1.223:3306/youth?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: youth_admin
        password: 1234

    jpa:
        show-sql: true
        database-platform: org.hibernate.dialect.MySQL8Dialect
        database: mysql

        hibernate:
            ddl-auto: none
            naming:
                strategy: org.hibernate.cfg.ImprovedNamingStrategy
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

        generate-ddl: false

        properties:
            hibernate:
                format_sql: true
                enable_lazy_load_no_trans: true

    mail:
        host: smtp.gmail.com
        port: 587
        username: wogks119@gmail.com
        password: fsakvctwlpgwglpd
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
                        required: true
                    connectiontimeout: 5000
                    timeout: 5000
                    writetimeout: 5000
        auth-code-expiration-millis: 600000  # 10 * 60 * 1000 == 10분
    data:
        redis:
            host: localhost
            port: 6379
server:
  port: 5000


